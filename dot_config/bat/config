# Dark theme optimized for terminals (Nord or OneHalfDark popular for coding)
--theme="Nord"


# Style: line numbers, Git changes, full header (filename/size), subtle grid
--style="numbers,changes,header-filename,header-filesize,grid"


# Tabs: 4 spaces (common for Go/Python/Shell/K8s YAML)
--tabs=4

# Pager: less with raw ANSI, quit-if-one-screen, no-init (fast for small files)
--pager="less -RF"

# Enable italics if terminal supports (e.g., for comments)
--italic-text=always

# Always colorize (useful for piping to tmux/screen)
--color=always


# Map common DevOps files (e.g., YAML configs, Helm, Terraform)
--map-syntax="*.yaml:yaml"
--map-syntax="*.yml:yaml"
--map-syntax="*.tf:Terraform"
--map-syntax="*.gotmpl:Go HTML Template"
--map-syntax="Dockerfile:Dockerfile"
--map-syntax="*.toml:TOML"

# Squeeze multiple blank lines (clean IaC/logs)
--squeeze-blank

# Highlight non-printables (handy for configs/logs)
--show-all
